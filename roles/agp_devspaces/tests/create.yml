---
- name: Deploy devspaces
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Execute role
      ansible.builtin.include_role:
        name: jeffcpullen.agnosticp.agp_devspaces
      vars:
        ACTION: 'create'
